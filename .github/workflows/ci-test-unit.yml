name: Test Unit

on:
  workflow_call:
  workflow_dispatch:

concurrency:
  group: unit-${{ github.workflow }}-${{ github.ref == 'refs/heads/main' && github.run_id || github.ref }}
  cancel-in-progress: true

jobs:
  unit-test:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository }}:latest
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    timeout-minutes: 20
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          clean: true

      - name: Setup Test Environment
        uses: ./.github/actions/setup-test-env

      - name: Run unit tests
        shell: bash
        run: |
          set -o pipefail
          mkdir -p job_logs
          cd client
          npm run github:test:unit 2>&1 | tee -a ../job_logs/unit-test.log
        env:
          CI: true
          NODE_ENV: test
          TEST_ENV: localhost
          DISPLAY: :99
          DISABLE_POSTHOG: true
          VITE_DISABLE_FLUID_TELEMETRY: true

      - name: Summarise failures
        if: failure()
        run: |
          echo "## ❌ Unit Test Failed" >> $GITHUB_STEP_SUMMARY
          if [ -f "job_logs/unit-test.log" ]; then
             grep -E -A1 -B0 "FAIL|FAILED" "job_logs/unit-test.log" >> $GITHUB_STEP_SUMMARY || true
          fi

      - name: Upload logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-logs
          path: job_logs/
          retention-days: 2
