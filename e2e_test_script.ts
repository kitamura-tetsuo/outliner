import { expect, test } from "@playwright/test";

test("create layout and see if empty items exist", async ({ page }) => {
    // just a dummy script to run in the e2e environment to see what the layout renders
});
