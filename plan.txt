1. **Implement `iterateItemsDeep` in `shared/src/utils/itemTraversal.ts`**:
   - Add a new function `iterateItemsDeep` that performs a depth-first search (using an explicit stack) over the item subtree. It will take an item collection (e.g., `pageItem.items`) and yield all items, including nested ones.
   - Using an explicit stack ensures safety against very deep outlines (avoids Maximum Call Stack Size Exceeded errors).

2. **Update `client/src/lib/backlinkCollector.ts`**:
   - Import `iterateItemsDeep` from `../utils/itemTraversal` or `../../shared/src/utils/itemTraversal` (need to check the exact path).
   - Replace the single-level `iterateItems` loop with `iterateItemsDeep` when checking child items.
   - Keep the existing `itemText.includes("[")` fast path before the regex and skip empty `Y.Text` values as requested.

3. **Update tests**:
   - In `client/src/lib/backlinkCollector.test.ts`, add a test case where the linking item is two levels below the page item (a nested child).
   - Run `client/src/lib/backlinkCollector.perf.test.ts` to ensure it still passes.

4. **Verify the fix**:
   - Run unit tests in `client` via `npm run test:unit`.
   - Run `npm run check` and `npm run lint` in `client`.

5. **Complete pre commit steps**:
   - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
