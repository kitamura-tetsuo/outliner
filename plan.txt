1. Delete test-seam mock functions in `client/src/lib/yjs/service.ts`:
   - Remove `resolveOverlayStore()` and use `editorOverlayStore` directly.
   - Remove `resolvePresenceStore()` and use `presenceStore` directly.
   - Remove the `globalThis` check in `resolveUserColor` and use `colorForUser` directly, or simply remove `resolveUserColor` if it's identical to `colorForUser`.

2. Update `applyPresenceToOverlay` in `client/src/lib/yjs/service.ts`:
   - Replace the `window.appStore.currentPage` check. Instead, we can optionally pass `currentPageId` to `applyPresenceToOverlay`, or import `store` from `../../stores/store.svelte` and read `store.currentPage?.id` directly. The issue description states: "Replace the window.appStore.currentPage read with an explicit import of the app store, or — better for testability — pass the current page id into applyPresenceToOverlay() from bindProjectPresence's caller." Given `bindProjectPresence` might not easily get `currentPageId` dynamically as it registers an event listener, let's explore if we can pass it, otherwise fallback to importing `store`. Wait, `applyPresenceToOverlay` is called from an `awareness.on("change")` handler inside `bindProjectPresence`, so passing it from the caller of `bindProjectPresence` (which is run once) is incorrect, it must be dynamic. The best option to evaluate at the time of awareness update is passing a getter or importing `store` directly. Let's import `store` directly as suggested as an option.

3. Update tests in `client/src/lib/yjs/service.test.ts`:
   - Remove the `globalThis` assignments.
   - Use `vi.mock` for `presenceStore`, `editorOverlayStore`, `colorForUser` instead.

4. Verify tests pass:
   - Run `npx vitest run src/lib/yjs/service.test.ts`
