/** @feature AUT-0000 */
const { describe, it } = require("mocha");
const { expect } = require("chai");

describe("認証サービスの基本テスト", () => {
    it("基本的なテストが実行できる", () => {
        expect(true).to.be.true;
    });
});
