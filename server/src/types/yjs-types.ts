/**
 * Yjs data-structure types are defined once in the shared workspace.
 * Re-exported here so existing `../types/yjs-types` imports keep working.
 */
export * from "../../../shared/src/types/yjs-types.js";
