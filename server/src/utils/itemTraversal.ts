/**
 * Item traversal helpers are defined once in the shared workspace.
 * Re-exported here so existing `../utils/itemTraversal` imports keep working.
 */
export * from "../../../shared/src/utils/itemTraversal.js";
