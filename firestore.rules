rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{projectId} {
      allow read: if request.auth.uid == resource.data.ownerId || resource.data.permissions.exists(p, p.userId == request.auth.uid);
      allow write: if false;
    }
  }
}
