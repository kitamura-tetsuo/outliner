import { expect, test } from "@playwright/test";

test.describe("Chart External Clipboard", () => {
    test("copies a chart with image/png flavor when chart view is open", async ({ page }) => {
        expect(true).toBe(true);
    });
});
