--- client/src/routes/[project]/[page]/+page.svelte
+++ client/src/routes/[project]/[page]/+page.svelte
@@ -243,6 +243,8 @@
                     { error: new Error(`Failed to find or create page "${pageName}"`) },
                     "loadProjectAndPage: Failed to find or create page",
                 );
+                _isLoading = false;
+                return;
             }
         } catch (err) {
             logger.error({ error: err }, "Failed to load project and page:");
@@ -560,11 +562,11 @@
         />
     </div>

-    {#if _isLoading && !store.currentPage}
+    {#if _isLoading && !pageNotFound && !store.currentPage}
         <div class="flex flex-col items-center justify-center py-8 space-y-4" aria-busy="true" aria-live="polite" role="status">
             <div class="loader" aria-hidden="true"></div>
             <div class="text-gray-600 text-sm font-medium">Loading Page...</div>
         </div>
-    {:else if !error}
+    {:else if !error && store.currentPage}
         <!-- Always mount OutlinerBase, switch display internally based on pageItem presence -->
         {#key lastReset}
