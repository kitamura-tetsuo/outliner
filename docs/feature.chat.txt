### 📑 Context
`/workspace/docs/client-features.yaml` に定義されている下記機能を実装して、テストを作成・追加してください。
IME-0001

### 🛠 Requirements
1. テスト内容に応じて、unit/e2e testを使い分けて書いて下さい。。
  unit testは vitestを使用してください。
  e2e testは playwright を使用してください。
  一つの機能のテストは一つにして下さい。
  UIのテストは e2eで実施して下さい。
2. unit testのテストファイルはテスト対象と同じフォルダに置いて下さい。
  unit testの拡張子は .spec.ts
  e2e testは `/workspace/client/e2e/core/<FeatureID>.spec.ts` に置いて下さい。
    例: `/workspace/client/e2e/core/FTR-0012.spec.ts`
3. 各テストファイルの冒頭に次の例に沿ったメタを入れて下さい。
   /** @feature FTR-0012
    *  Title   : User can reset forgotten password
    *  Source  : docs/client-features.yaml
    */
4. テストファイル作成後、client-features.yamlのテストを追加した機能 tests欄へ、テストファイルのパスを追加してください。

他、必要な手順や情報があればご提案ください。指示のためのテンプレートを作成中です。





### 📑 Context
`/workspace/docs/client-features.yaml` に定義されている status: implementedであり、テストが空の機能に対してテストを作成・追加してください。

### 🛠 Requirements
1. テスト内容に応じて、unit/e2e testを使い分けて書いて下さい。。
  unit testは vitestを使用してください。
  e2e testは playwright を使用してください。
  一つの機能のテストは一つにして下さい。
  UIのテストは e2eで実施して下さい。

2. unit testのテストファイルはテスト対象と同じフォルダに置いて下さい。
  unit testの拡張子は .spec.ts
  e2e testは `/workspace/client/e2e/core/<FeatureID>.spec.ts` に置いて下さい。
    例: `/workspace/client/e2e/core/FTR-0012.spec.ts`
3. 各テストファイルの冒頭に次の例に沿ったメタを入れて下さい。
   /** @feature FTR-0012
    *  Title   : User can reset forgotten password
    *  Source  : docs/client-features.yaml
    */
4. テストファイル作成後、client-features.yamlのテストを追加した機能 tests欄へ、テストファイルのパスを追加してください。

他、必要な手順や情報があればご提案ください。指示のためのテンプレートを作成中です。


### ✅ Scope
- **対象 Feature IDs**
  - FTR-0012 – User can reset forgotten password
  - FTR-0015 – Two-factor authentication  
    （いずれも `status: implemented` かつ `tests: []`）




📄 依頼テンプレート（GitHub Issue／Pull-Request／Copilot Chat いずれでも可）

## Task
Convert the following plain-text table into new **cursor-movement** feature entries
in `/workspace/docs/client-features.yaml`, **without touching any other sections**.

## Source table
基本のカーソル移動
操作名 | ショートカットキー | 操作内容 (カーソル移動)
左へ移動 | ← | カーソルを1文字左に移動します。

YAML format

Each entry should follow this schema exactly (keys in this order):

- id: CLM-XXXX        # 4-digit sequential number, zero-padded
  title: "<操作名>"
  shortcut: "<ショートカットキー>"
  description: "<操作内容>"
  category: cursor-movement
  status: drafted     # leave as drafted; dev will flip to implemented later
  tests: []           # stay empty; separate issue will add spec files

Use the next available ID after the current highest CLM-####.
Append new items under the cursor-movement section; create the section if absent.
Keep existing YAML indentation (2 spaces).
Add the following cursor-movement entries to client-features.yaml







### 📑 Context
`/workspace/docs/client-features.yaml` に定義されている status: implementedであり、テストが空の機能に対してテストを作成・追加してください。

### 🛠 Requirements
1. テスト内容に応じて、unit/component/e2e testを使い分けて書いて下さい。。
  unit testは vitestを使用してください。
  component testは playwright を使用してください。https://playwright.dev/docs/test-components
  e2e testは playwright を使用してください。
  一つの機能のテストは一つにして下さい。
  UIのテストはcomponent又はe2eで実施して下さい。

2. unit/component testのテストファイルはテスト対象と同じフォルダに置いて下さい。
  unit testの拡張子は .spec.ts
  component testの拡張子は .test.ts
  e2e testは `/workspace/client/e2e/core/<FeatureID>.spec.ts` に置いて下さい。
    例: `/workspace/client/e2e/core/FTR-0012.spec.ts`
3. 各テストファイルの冒頭に次の例に沿ったメタを入れて下さい。
   /** @feature FTR-0012
    *  Title   : User can reset forgotten password
    *  Source  : docs/client-features.yaml
    */
4. テストファイル作成後、client-features.yamlのテストを追加した機能 tests欄へ、テストファイルのパスを追加してください。

他、必要な手順や情報があればご提案ください。
